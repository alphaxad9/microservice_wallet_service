apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-rabbitmq-consumer
  labels:
    app: wallet-service
    component: rabbitmq-consumer
    managed-by: {{ .Release.Service }}
spec:
  replicas: {{ .Values.deployments.rabbitmqConsumer.replicas }}
  selector:
    matchLabels:
      app: wallet-service
      component: rabbitmq-consumer
  template:
    metadata:
      labels:
        app: wallet-service
        component: rabbitmq-consumer
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      containers:
      - name: wallet-rabbitmq-consumer
        image: {{ .Values.backend.wallet.image.repository }}:{{ .Values.backend.wallet.image.tag }}
        imagePullPolicy: {{ .Values.backend.wallet.image.pullPolicy }}
        command: ["python", "manage.py", "run_consumer"]
        envFrom:
        - configMapRef:
            name: {{ .Values.configMap.name }}
        - secretRef:
            name: {{ .Values.secrets.wallet }}
        - secretRef:
            name: {{ .Values.secrets.postgres }}
        - secretRef:
            name: {{ .Values.secrets.rabbitmq }}
        - secretRef:
            name: {{ .Values.secrets.kafka }}
        - secretRef:
            name: {{ .Values.secrets.redis }}
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: SERVICE_COMPONENT
          value: "rabbitmq-consumer"
        resources:
          requests:
            memory: {{ .Values.deployments.rabbitmqConsumer.resources.requests.memory }}
            cpu: {{ .Values.deployments.rabbitmqConsumer.resources.requests.cpu }}
          limits:
            memory: {{ .Values.deployments.rabbitmqConsumer.resources.limits.memory }}
            cpu: {{ .Values.deployments.rabbitmqConsumer.resources.limits.cpu }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000