apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm.appname" (dict "ctx" $ "svc" "wallet") }}
  labels:
    app: {{ include "helm.appname" (dict "ctx" $ "svc" "wallet") }}
spec:
  selector:
    app: {{ include "helm.appname" (dict "ctx" $ "svc" "wallet") }}
  ports:
    - protocol: TCP
      port: {{ .Values.backend.wallet.port }}
      targetPort: {{ .Values.backend.wallet.port }}
  type: ClusterIP